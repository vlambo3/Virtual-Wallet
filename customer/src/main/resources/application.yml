server:
  port: ${PORT:${SERVER_PORT:0}}

spring:
  application:
    name: ms-customer
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/customer-dev
#    username: usr-customer
#    password: pwd-customer
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
